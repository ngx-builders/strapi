module.exports = function (grunt) {
	grunt.loadNpmTasks('../../../node_modules/grunt-exec');

	return {
		ttsc: {
			cmd: 'npx ttsc -p apps/<%= name %>/tsconfig.json --listEmittedFiles --extendedDiagnostics'
		}
	}
};
